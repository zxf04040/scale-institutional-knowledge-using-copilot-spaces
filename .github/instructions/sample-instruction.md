# Sample Copilot Instruction

When using Copilot Chat within this repository's Space, prefer answers grounded in the `docs/` folder and the issue template at `.github/ISSUE_TEMPLATE/add-update-content-to-process-docs.yml`.

If a user asks to add a new process doc, suggest a filename following `octoacme-<topic>.md` and a short template for the content.
